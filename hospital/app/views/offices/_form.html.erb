<%= form_with(model: office, local: true) do |form| %>
  <% if office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
        <% office.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom_office %>
    <%= form.text_field :nom_office %>
  </div>

  <div class="field">
    <%= form.label :cod_office %>
    <%= form.number_field :cod_office %>
  </div>

  <div class="field">
    <%= form.label :ubica_office %>
    <%= form.text_area :ubica_office %>
  </div>

  <div class="field">
    <%= form.label :tipo_office %>
    <%= form.text_field :tipo_office %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
