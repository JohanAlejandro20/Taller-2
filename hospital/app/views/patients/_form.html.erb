<%= form_with(model: patient, local: true) do |form| %>
  <% if patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
        <% patient.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom_patient %>
    <%= form.text_field :nom_patient %>
  </div>

  <div class="field">
    <%= form.label :ape_patient %>
    <%= form.text_field :ape_patient %>
  </div>

  <div class="field">
    <%= form.label :cc_patient %>
    <%= form.number_field :cc_patient %>
  </div>

  <div class="field">
    <%= form.label :fecha_nacimiento %>
    <%= form.date_select :fecha_nacimiento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
